apiVersion: v1
kind: Service
metadata:
  name: {{ include "devops-app.fullname" . }}-api
  labels:
    {{- include "devops-app.labels" . | nindent 4 }}
    app.kubernetes.io/component: api
spec:
  type: {{ .Values.api.service.type }}
  selector:
    {{- include "devops-app.componentLabels" (dict "root" . "component" "api") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.api.service.port }}
      targetPort: http
      protocol: TCP

